The server for a MIT build app called, Bridge. The main purpose is to connect older people. it uses HTTP websites and POST.

The app itself is a project and will never be released

The server uses the HTTP protocol on port 3000.

This is a project and is not not ab
